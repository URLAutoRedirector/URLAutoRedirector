{
  "name": "urlautoredirector",
  "version": "1.18.1",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "release:build": "node scripts/release.mjs build",
    "release:tag": "node scripts/release.mjs tag",
    "release:changelog": "node scripts/release.mjs changelog",
    "check": "node --check src/background.js && node --check src/options.js && node --check scripts/release.mjs",
    "test": "node --test"
  },
  "devDependencies": {
    "archiver": "^7.0.1",
    "jsdom": "^29.1.1",
    "yaml": "^2.8.1"
  }
}
